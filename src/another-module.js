export const another_var = 500;

// maybe no?
// console.log(`can I read exported variable: ${my_var}`);

// maybe yes because the importer of this module also imports the function from the top-level module. so it should be available here? which is weired
my_awesome_func();
